# 🌊 Auditoria ASF App — 22/09/2026
_Executada via Composio (GitHub API) sobre a branch `main`._

## ✅ O que foi verificado
- Estrutura do repositório: 182 arquivos mapeados.
- Todos os 23 links internos de `index.html` (href/src para .html/.js/.css/.json/.png/.svg etc.) — **nenhum quebrado**.
- `carteirinha.js`, `verificar.html`, `hospedagens/index.html`, `data/hospedagens.json`, `data/events.json`.
- `data/hospedagens.json`: estrutura pronta e **corretamente vazia** (sem parcerias inventadas) ✅ — alinhado à regra "não inventar parcerias".
- `verificar.html`: validação por padrão de formato — OK como primeira camada.

## 🔧 Correções aplicadas neste commit
1. **Remoção de publicidade (alinhamento ao propósito sem fins lucrativos):**
   - Excluído `ads.txt` (declaração de vendedor de anúncios).
   - Adicionado workflow `.github/workflows/remove-adsense.yml` + script `.github/scripts/remove_adsense.py` que removem do `index.html` o script do Google AdSense (`ca-pub-3811729023785282`), os `preconnect`/`dns-prefetch` de `pagead2.googlesyndication.com` e `googletagservices.com` e o comentário "Monetização consolidada". **Rodar manualmente uma vez em Actions → "ASF - Remover AdSense" → Run workflow** (o `index.html` tem ~940 KB e não pôde ser editado via API).
2. **Carteirinha ASF — segurança e LGPD (`carteirinha.js`):**
   - Número da associada agora gerado com `crypto.getRandomValues` (imprevisível), alfabeto sem caracteres ambíguos.
   - Checkbox de consentimento LGPD obrigatório antes de gerar a carteirinha, com link para `privacidade.html`.
   - Registro do timestamp do consentimento; dados continuam **somente no dispositivo** (localStorage) e podem ser apagados em "Editar".

## ⚠️ Pendências / prioridades (não alteradas sem decisão da ASF)
1. **GA4 placeholder**: `gtag('config','G-XXXXXXXXXX')` ainda no código — criar propriedade GA4 real ou remover o snippet.
2. **Monetização remanescente em `index.html`**: loja de afiliados (`renderAffiliateStore`, tracking `affiliate_click`, `ASFMonetization`) e modal "Indique & Ganhe". Recomendo remover ou converter em "Loja solidária / projetos com arrecadação pontual" — decisão editorial da ASF.
3. **Tabela de controle de associadas**: hoje a carteirinha é 100% local. Para carteirinha oficial com verificação real, é preciso backend (há rascunho em `docs/ASF_App_Data.gs` / Google Sheets). Em GitHub Pages puro, Google Apps Script como API é o caminho sem servidor.
4. **Verificação real da carteirinha**: `verificar.html` valida apenas formato; integrar com a planilha/API de associadas quando o backend existir.
5. **Parcerias de Estadias pelo Mundo**: estrutura pronta em `data/hospedagens.json` — popular somente com parcerias confirmadas pela ASF.
6. **`href="#"` sem `return false`** e dicas com aspas simples quebrando parse (ver `docs/TAREFAS-ASF.md`, BUG-2 e BUG-7).

## 📌 Regras respeitadas
- Nenhuma estrutura existente alterada além do necessário.
- Nenhum dado, métrica ou parceria inventada.
- Publicação permanece via GitHub Pages.
